Solution contains class with Filter method. Filter method gets array of integers and digit. Then it scans received array
for numbers which contains specified digit and puts them in new array. 
There are tests for method in project also. 
